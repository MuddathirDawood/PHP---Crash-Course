<?php
    // ECHO - Output various data types
    // echo "Hello World!";

    // print - Works like echo, but can only take a single argument
    // print 12345;

    // print_r() - Print single values and arrays
    // print_r([1,2,3,4,5]);

    // var_dump() - Returns more info like data type and length
    // var_dump('Hello');
    
    // var_export() - Similar to var_dump(). Outputs a string reprsentation of a variable
    // var_export('Hello');



    /*
        If there is only php in the file then there is no need for a php closing tag (?>) BUT
        If there is HTML in the file as well then the closing tag will be present
    */